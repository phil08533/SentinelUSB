# SentinelUSB Live Image

This directory will contain the minimal Debian live-build configuration for the bootable SentinelUSB image.

The first image intentionally stays small: terminal-first operation, a lightweight optional GUI, ClamAV/YARA scanning, Windows-volume detection, read-only mounting, and reporting.

Build instructions will be added with the first reproducible build configuration.